import { base } from '$app/paths';

const links = {
	legal: {
		privacy: `${base}/legal/privacy`,
		terms: `${base}/legal/terms`,
		optOut: `${base}/legal/opt-out`
	},
	home: `${base}/`
};

export { links };
